[project]
name = "companion-core"
version = "0.1.0"
description = "汎用実況基盤 (ゲーム非依存)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["core"]

[tool.hatch.build.targets.wheel.sources]
"core" = "companion_core"
